name: Test CWL

on:
  push:
  pull_request:
  workflow_dispatch:
jobs:
  list-tests:
    runs-on: ubuntu-latest
    outputs:
      tests-matrix: ${{ steps.set-matrix.outputs.tests-matrix }}
    steps:
    - uses: actions/checkout@v3
    - id: set-matrix
      run: |
        echo "tests-matrix=$(ls tests/*.cwl | jq -R -s -c 'split("\n")[:-1] | map([., (sub("\\.cwl$"; ".yml"))])')"  >> "$GITHUB_OUTPUT"
    - name: Show matrix
      run: |
        echo ${{ steps.set-matrix.outputs.tests-matrix }}
  test-cwl:
    needs: list-tests
    strategy:
        matrix:
          os: [ubuntu-latest, macos-latest]
          tests: ${{ fromJson(needs.list-tests.outputs.tests-matrix) }}
    runs-on: ${{ matrix.os }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Setup Python
      run: |
        pip install cwltool
    - name: Setup Docker
      if: ${{ matrix.os == 'macos-latest' }}
      run: |
        brew install docker
    - name: Get CWL Version
      run: |
        echo "# cwl version" >> $GITHUB_STEP_SUMMARY
        echo "`$(cwltool --version)`" >> $GITHUB_STEP_SUMMARY
    - name: Get Docker Version
      run: |
        echo "# docker version" >> $GITHUB_STEP_SUMMARY
        echo "`$(docker --version)`" >> $GITHUB_STEP_SUMMARY
    - name: Run CWL
      run: |
        echo "# Run tests" >> $GITHUB_STEP_SUMMARY
        echo " ${{ matrix.tests[0] }} " >> $GITHUB_STEP_SUMMARY
        echo cwltool --verbose --docker ${{ matrix.tests[0] }} --input ${{ matrix.tests[1] }}